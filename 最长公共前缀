class Solution(object):
    def longestCommonPrefix(self, strs):
        """
        :type strs: List[str]
        :rtype: str
        """
        result = ''
        for i in zip(*strs):
            tmp = set(i)
            if len(tmp) == 1:
                result += i[0]
            else:
                break
        return result
